export type PostType = {
    id: string; // url prop will be used as the id of each post
    title: string;
    description: string;
    url: string;
    publishedAt: string;
    content: string;
    source: string;
  };